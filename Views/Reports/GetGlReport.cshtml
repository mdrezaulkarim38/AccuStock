@model IEnumerable<AccuStock.Models.ViewModels.GeneralLedger.GLedger>

@{
    ViewData["Title"] = "General Ledger Report";
    var fromDate = ViewBag.FromDate as string;
    var toDate = ViewBag.ToDate as string;
    var selectedReportType = ViewBag.ReportType as string;
}

<div class="row">
    <div class="col-md-12">
        <div class="card mb-4">
            <div class="card-header">
                <h5>Filters</h5>
            </div>
            <div class="card-body">
                <form asp-action="GetGlReport" method="post" class="row g-3">
                    <div class="col-md-2">
                        <label for="startDate" class="form-label">From Date</label>
                        <input type="date" name="startDate" class="form-control" value="@fromDate" required />
                    </div>
                    <div class="col-md-2">
                        <label for="endDate" class="form-label">To Date</label>
                        <input type="date" name="endDate" class="form-control" value="@toDate" required />
                    </div>
                    <div class="col-md-2">
                        <label for="BranchId" class="form-label">Branch</label>
                        <select class="form-select" name="branchId">
                            <option value="">All</option>
                            @if (ViewBag.Branches != null)
                            {
                                foreach (var branch in ViewBag.Branches)
                                {
                                    <option value="@branch.Id">@branch.Name</option>
                                }
                            }
                            else
                            {
                                <option disabled>No branches available</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label for="chartOfAccountId" class="form-label">Account</label>
                        <select class="form-select" name="chartOfAccountId">
                            <option value="">All</option>
                            @foreach (var coa in ViewBag.ChartOfAccounts)
                            {
                                <option value="@coa.Id">@coa.Name</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label for="reportType" class="form-label">Report Type</label>
                        <select name="reportType" class="form-select">
                            @{
                                var reportOptions = new[] { "PDF", "Excel" };
                                foreach (var option in reportOptions)
                                {
                                    var selected = option == selectedReportType ? "selected" : "";
                                    <option value="@option" selected>@option</option>
                                }
                            }
                        </select>
                    </div>
                    <div class="col-md-2 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary w-100">Generate</button>
                    </div>
                </form>
            </div>
        </div>

        @if (Model != null && Model.Any())
        {
            <div class="card shadow-sm">
                <div class="card-header text-center">
                    <h6 class="text-muted mb-1">Demo Org</h6>
                    <h4 class="fw-bold">General Ledger</h4>
                    <h6>From: @fromDate - To: @toDate</h6>
                    <h6>Basis: @selectedReportType</h6>
                </div>
                <div class="card-body">
                    <table class="table table-bordered table-hover table-striped">
                        <thead class="table-dark">
                            <tr>
                                <th>Account</th>
                                <th>Vch No</th>
                                <th>Vch Date</th>
                                <th>Description</th>
                                <th>Debit</th>
                                <th>Credit</th>
                                <th>Remarks</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var entry in Model)
                            {
                                <tr>
                                    <td>@entry.ChartOfAccount?.Name (@entry.ChartOfAccount?.AccountCode)</td>
                                    <td>@entry.JournalPostDetail?.VchNo</td>
                                    <td>
                                        @if (entry.JournalPostDetail?.VchDate != null)
                                        {
                                            @entry.JournalPostDetail.VchDate.Value.ToString("dd/MM/yyyy")
                                        }
                                    </td>
                                    <td>@entry.JournalPostDetail?.Description</td>
                                    <td>@String.Format("{0:N2}", entry.JournalPostDetail?.Debit)</td>
                                    <td>@String.Format("{0:N2}", entry.JournalPostDetail?.Credit)</td>
                                    <td>@entry.JournalPostDetail?.Remarks</td>
                                </tr>
                            }
                        </tbody>
                        <tfoot class="table-light fw-bold">
                            <tr>
                                <td colspan="4" class="text-end">Total</td>
                                <td>
                                    @String.Format("{0:N2}", Model.Sum(x => x.JournalPostDetail?.Debit ?? 0))
                                </td>
                                <td>
                                    @String.Format("{0:N2}", Model.Sum(x => x.JournalPostDetail?.Credit ?? 0))
                                </td>
                                <td></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        }
    </div>
</div>
